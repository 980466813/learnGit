Git is a version control system.
Git is free software.

Study Git

1. git status

用于掌握工作区的状态

2. git diff

用于查看修改内容

3. git log 以及 git log -pretty=oneline

用于查看过去提交历史，以及格式化输出

4. git reset --hard HEAD^

用于回退到上个版本 HEAD^代表上个版本，同理上上个版本就是HEAD^^,往前n个版本则可以写为HEAD~100

5. git reflog

用于记录每一次命令
